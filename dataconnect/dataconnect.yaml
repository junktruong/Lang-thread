specVersion: "v1"
serviceId: "lang-thread"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lang-thread-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
